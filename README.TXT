ejecutar servidor: flask run
ejecutar programa phyton: python3 app.py
actualizar base de datos: sqlite3 componentes.db < setup_db.sql
cada vez que quiera actualizar el github: git push -u origin master
pip install -r requirements.txt 
git add .
git commit -m "mensaje"
git push -u origin main
